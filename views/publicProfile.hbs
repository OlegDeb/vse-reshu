<h2>Публичный профиль пользователя</h2>

<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">
                    {{#if user.firstName}}
                        {{user.firstName}}
                        {{#if user.lastName}}
                            {{user.lastName}}
                        {{/if}}
                    {{else}}
                        {{user.username}}
                    {{/if}}
                </h5>

                {{#if user.firstName}}
                <p class="card-text">
                    <strong>Имя:</strong> {{user.firstName}}
                    {{#if user.lastName}}
                        {{user.lastName}}
                    {{/if}}
                </p>
                {{/if}}

                <p class="card-text">
                    <strong>Логин:</strong> {{user.username}}
                </p>

                <p class="card-text">
                    <strong>Дата регистрации:</strong> {{user.createdAtFormatted}}
                </p>

                {{#if isOwner}}
                <div class="alert alert-info mt-3">
                    <strong>Это ваш профиль</strong><br>
                    <a href="/profile" class="btn btn-primary btn-sm mt-2">Перейти в личный кабинет</a>
                </div>
                {{/if}}

                <!-- Здесь в будущем можно добавить разделы проектов, задач, отзывов -->
                <div class="mt-4">
                    <h6>Публичная активность</h6>
                    <p class="text-muted">Разделы проектов, задач и отзывов будут добавлены позже.</p>
                </div>
            </div>
        </div>
    </div>

    <div class="col-md-4">
        <div class="d-grid gap-2">
            <a href="/" class="btn btn-secondary">На главную</a>
            {{#if isOwner}}
                <a href="/profile/edit" class="btn btn-outline-primary">Редактировать профиль</a>
            {{/if}}
        </div>

        <!-- Здесь можно добавить блок ссылок на другие публичные страницы пользователя -->
        <div class="mt-4">
            <h6>Публичные страницы</h6>
            <div class="list-group">
                <a href="/users/{{user.username}}" class="list-group-item list-group-item-action active">
                    Основной профиль
                </a>
                <!-- В будущем здесь будут ссылки на проекты, задачи и отзывы -->
                <a href="#" class="list-group-item list-group-item-action disabled">
                    Проекты (скоро)
                </a>
                <a href="#" class="list-group-item list-group-item-action disabled">
                    Задачи (скоро)
                </a>
                <a href="#" class="list-group-item list-group-item-action disabled">
                    Отзывы (скоро)
                </a>
            </div>
        </div>
    </div>
</div>