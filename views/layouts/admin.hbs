<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Иконки Bootstrap -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <!-- Основные стили -->
    <link href="/css/style.css" rel="stylesheet">
    <style>
        .admin-layout {
            display: flex;
            min-height: 100vh;
            padding-top: 56px; /* Высота navbar */
        }
        
        .admin-layout .navbar {
            padding-left: 30px;
            padding-right: 30px;
        }
        
        .navbar {
            padding-left: 30px !important;
            padding-right: 30px !important;
        }
        
        .admin-sidebar {
            width: 250px;
            background-color: #ffffff;
            border-right: 1px solid #e0e0e0;
            position: fixed;
            left: 0;
            top: 56px;
            height: calc(100vh - 56px);
            overflow-y: auto;
            z-index: 999;
            box-shadow: 2px 0 4px rgba(0,0,0,0.05);
        }
        
        .admin-content-wrapper {
            flex: 1;
            margin-left: 250px;
            background-color: #f5f5f5;
            min-height: calc(100vh - 56px);
            padding: 30px;
            box-sizing: border-box;
        }
        
        .admin-content {
            padding-top: 40px !important;
            padding-right: 2rem;
            padding-bottom: 2rem;
            padding-left: 2rem;
            max-width: 100%;
            background-color: #ffffff;
            min-height: calc(100vh - 56px - 60px); /* Вычитаем отступы сверху и снизу (30px * 2) */
            box-shadow: 0 0 10px rgba(0,0,0,0.05);
        }
        
        .admin-content > *:first-child {
            margin-top: 0;
        }
        
        .admin-nav-menu {
            padding: 50px 0 0 0;
        }
        
        .admin-nav-item {
            display: flex;
            align-items: center;
            width: 100%;
            padding: 0.75rem 1.5rem;
            color: #333;
            text-decoration: none;
            transition: all 0.2s ease;
            border-left: 3px solid transparent;
            box-sizing: border-box;
        }
        
        .admin-nav-item:hover {
            background-color: #f8f9fa;
            color: #1e88e5;
            border-left-color: #1e88e5;
        }
        
        .admin-nav-item.active {
            background-color: #e3f2fd;
            color: #1e88e5;
            border-left-color: #1e88e5;
            font-weight: 600;
        }
        
        .admin-nav-item i {
            width: 20px;
            margin-right: 0.75rem;
            font-size: 1.1rem;
            flex-shrink: 0;
        }
        
        .admin-nav-item span {
            flex: 1;
            white-space: nowrap;
        }
        
        @media (max-width: 768px) {
            .admin-sidebar {
                transform: translateX(-100%);
                transition: transform 0.3s ease;
            }
            
            .admin-sidebar.mobile-open {
                transform: translateX(0);
            }
            
            .admin-content-wrapper {
                margin-left: 0;
            }
            
            .admin-sidebar-toggle {
                display: block;
                position: fixed;
                top: 60px;
                left: 10px;
                z-index: 1001;
                background: white;
                border: 1px solid #ddd;
                border-radius: 4px;
                padding: 0.5rem;
            }
        }
        
        .admin-sidebar-toggle {
            display: none;
        }
    </style>
</head>
<body>
    <!-- Навигационная панель -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">
                <img src="/img/logo/logo.png" alt="Логотип" height="30" class="me-2">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Главная</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/tasks">Задания</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/articles">Статьи</a>
                    </li>
                </ul>
                <div class="d-flex align-items-center">
                    {{#if user}}
                        <div class="dropdown">
                            <a href="#" class="nav-link d-flex align-items-center dropdown-toggle" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                {{#if user.avatar}}
                                    <img src="{{user.avatar}}" alt="Аватар" class="rounded-circle me-2" style="width: 32px; height: 32px; object-fit: cover;">
                                {{else}}
                                    <i class="bi bi-person-circle me-1" style="font-size: 1.5rem;"></i>
                                {{/if}}
                                <span>Привет, {{#if user.firstName}}{{user.firstName}}{{else}}{{user.username}}{{/if}}!</span>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                                <li><a class="dropdown-item" href="/profile"><i class="bi bi-person me-2"></i>Профиль</a></li>
                                <li><a class="dropdown-item" href="/tasks/create"><i class="bi bi-plus-circle me-2"></i>Разместить задание</a></li>
                                {{#if isAdmin}}
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/admin/articles"><i class="bi bi-shield-check me-2"></i>Админка</a></li>
                                {{/if}}
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/logout"><i class="bi bi-box-arrow-right me-2"></i>Выход</a></li>
                            </ul>
                        </div>
                    {{else}}
                        <a href="/login" class="nav-link me-3">Войти</a>
                        <a href="/register" class="btn btn-primary">Регистрация</a>
                    {{/if}}
                </div>
            </div>
        </div>
    </nav>

    <div class="admin-layout">
        <!-- Кнопка для мобильного меню -->
        <button class="admin-sidebar-toggle" onclick="document.querySelector('.admin-sidebar').classList.toggle('mobile-open')">
            <i class="bi bi-list"></i>
        </button>
        
        <!-- Боковое меню -->
        <aside class="admin-sidebar">
            {{> menu}}
        </aside>
        
        <!-- Основной контент -->
        <div class="admin-content-wrapper">
            <main class="admin-content">
                {{{body}}}
            </main>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Закрытие мобильного меню при клике вне его
        document.addEventListener('click', function(event) {
            const sidebar = document.querySelector('.admin-sidebar');
            const toggle = document.querySelector('.admin-sidebar-toggle');
            
            if (window.innerWidth <= 768 && sidebar.classList.contains('mobile-open')) {
                if (!sidebar.contains(event.target) && !toggle.contains(event.target)) {
                    sidebar.classList.remove('mobile-open');
                }
            }
        });
    </script>
</body>
</html>

