<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    
    <!-- SEO Meta Tags -->
    <meta name="description" content="{{#if metaDescription}}{{metaDescription}}{{else}}Всё реши - платформа для поиска и предоставления услуг{{/if}}">
    {{#if metaKeywords}}<meta name="keywords" content="{{metaKeywords}}">{{/if}}
    <meta name="author" content="Всё реши">
    <meta name="robots" content="index, follow">
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="{{#if article}}article{{else}}website{{/if}}">
    <meta property="og:title" content="{{title}}">
    <meta property="og:description" content="{{#if metaDescription}}{{metaDescription}}{{else}}Всё реши - платформа для поиска и предоставления услуг{{/if}}">
    {{#if article}}{{#if article.image}}<meta property="og:image" content="http://localhost:3000{{article.image}}">{{/if}}{{/if}}
    <meta property="og:url" content="http://localhost:3000{{#if article}}{{article.url}}{{else}}{{/if}}">
    
    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="{{title}}">
    <meta name="twitter:description" content="{{#if metaDescription}}{{metaDescription}}{{else}}Всё реши - платформа для поиска и предоставления услуг{{/if}}">
    {{#if article}}{{#if article.image}}<meta name="twitter:image" content="http://localhost:3000{{article.image}}">{{/if}}{{/if}}
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Иконки Bootstrap -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <!-- Основные стили -->
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <!-- Навигационная панель -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white">
        <div class="container">
            <a class="navbar-brand" href="/">
                <img src="/img/logo/logo.png" alt="Логотип" height="30" class="me-2">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item">
                        <a class="nav-link {{#if (eq currentPath '/')}}active{{/if}}" href="/">Главная</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{#if (or (eq currentPath '/tasks') (contains currentPath '/tasks/'))}}active{{/if}}" href="/tasks">Задания</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{#if (or (eq currentPath '/articles') (contains currentPath '/articles/'))}}active{{/if}}" href="/articles">Статьи</a>
                    </li>
                </ul>
                <div class="d-flex align-items-center">
                    {{#if user}}
                        <div class="dropdown">
                            <a href="#" class="nav-link d-flex align-items-center dropdown-toggle" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                {{#if user.avatar}}
                                    <img src="{{user.avatar}}" alt="Аватар" class="rounded-circle me-2" style="width: 32px; height: 32px; object-fit: cover;">
                                {{else}}
                                    <i class="bi bi-person-circle me-1" style="font-size: 1.5rem;"></i>
                                {{/if}}
                                <span>Привет, {{#if user.firstName}}{{user.firstName}}{{else}}{{user.username}}{{/if}}!</span>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                                <li><a class="dropdown-item" href="/profile"><i class="bi bi-person me-2"></i>Профиль</a></li>
                                <li><a class="dropdown-item" href="/tasks/create"><i class="bi bi-plus-circle me-2"></i>Разместить задание</a></li>
                                {{#if isAdmin}}
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/admin/articles"><i class="bi bi-shield-check me-2"></i>Админка</a></li>
                                {{/if}}
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/logout"><i class="bi bi-box-arrow-right me-2"></i>Выход</a></li>
                            </ul>
                        </div>
                    {{else}}
                        <a href="/login" class="nav-link me-3">Войти</a>
                        <a href="/register" class="btn btn-primary">Регистрация</a>
                    {{/if}}
                </div>
            </div>
        </div>
    </nav>

    {{#unless (eq currentPath '/')}}
    {{#unless (contains currentPath '/admin')}}
    <!-- Заголовок страницы с хлебными крошками -->
    <section class="page-header">
        <div class="container">
            {{#unless article}}
            {{#unless page}}
            <div class="page-header-content">
                <h1>
                    {{#if (contains currentPath '/tasks/category/')}}
                        {{#if currentCategory}}
                            <i class="{{currentCategory.icon}} me-2"></i>{{currentCategory.name}}
                        {{else}}
                            {{title}}
                        {{/if}}
                    {{else if (eq currentPath '/tasks')}}
                        {{#if currentCategory}}
                            <i class="{{currentCategory.icon}} me-2"></i>{{currentCategory.name}}
                        {{else}}
                            {{title}}
                        {{/if}}
                    {{else}}
                        {{title}}
                    {{/if}}
                </h1>
            </div>
            {{/unless}}
            {{#if page}}
            <div class="page-header-content">
                <h1>{{page.title}}</h1>
            </div>
            {{/if}}
            {{/unless}}
            <nav aria-label="breadcrumb" class="page-header-breadcrumb">
                <ol class="breadcrumb justify-content-center">
                    <li class="breadcrumb-item"><a href="/">Главная</a></li>
                    {{#if (contains currentPath '/tasks/category/')}}
                        <li class="breadcrumb-item"><a href="/tasks">Задания</a></li>
                        {{#if currentCategory}}
                            <li class="breadcrumb-item active">{{currentCategory.name}}</li>
                        {{else}}
                            <li class="breadcrumb-item active">Категория</li>
                        {{/if}}
                    {{else if (eq currentPath '/tasks')}}
                        <li class="breadcrumb-item active">Задания</li>
                    {{else if (contains currentPath '/tasks/')}}
                        <li class="breadcrumb-item"><a href="/tasks">Задания</a></li>
                        {{#if (eq currentPath '/tasks/create')}}
                            <li class="breadcrumb-item active">Создание задания</li>
                        {{else if (eq currentPath '/tasks/my-tasks')}}
                            <li class="breadcrumb-item active">Мои задания</li>
                        {{else}}
                            <li class="breadcrumb-item active">Просмотр задачи</li>
                        {{/if}}
                    {{else if (eq currentPath '/articles')}}
                        <li class="breadcrumb-item active">Статьи</li>
                    {{else if (eq currentPath '/profile')}}
                        <li class="breadcrumb-item active">Профиль</li>
                    {{else if (eq currentPath '/login')}}
                        <li class="breadcrumb-item active">Вход</li>
                    {{else if (eq currentPath '/register')}}
                        <li class="breadcrumb-item active">Регистрация</li>
                    {{else if article}}
                        <li class="breadcrumb-item"><a href="/articles">Статьи</a></li>
                        {{#if currentCategory}}
                            <li class="breadcrumb-item"><a href="/articles?category={{currentCategory.slug}}">{{currentCategory.name}}</a></li>
                        {{/if}}
                        <li class="breadcrumb-item active">{{article.title}}</li>
                    {{else if page}}
                        <li class="breadcrumb-item active">{{page.title}}</li>
                    {{else}}
                        {{#if breadcrumbs}}
                            {{#each breadcrumbs}}
                                {{#if this.url}}
                                    <li class="breadcrumb-item"><a href="{{this.url}}">{{this.name}}</a></li>
                                {{else}}
                                    <li class="breadcrumb-item active">{{this.name}}</li>
                                {{/if}}
                            {{/each}}
                        {{else}}
                            <li class="breadcrumb-item active">{{title}}</li>
                        {{/if}}
                    {{/if}}
                </ol>
            </nav>
        </div>
    </section>
    {{/unless}}
    {{/unless}}

    <main>
        {{{body}}}
    </main>

    <!-- Футер -->
    <footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-md-3 mb-4">
                    <h5>Всё реши</h5>
                    <ul class="list-unstyled">
                        <li><a href="#">О компании</a></li>
                        <li><a href="#">Помощь</a></li>
                    </ul>
                </div>
                <div class="col-md-3 mb-4">
                    <h5>Для заказчиков</h5>
                    <ul class="list-unstyled">
                        <li><a href="/tasks/create">Разместить задание</a></li>
                        <li><a href="/tasks">Каталог заданий</a></li>
                    </ul>
                </div>
                <div class="col-md-3 mb-4">
                    <h5>Для исполнителей</h5>
                    <ul class="list-unstyled">
                        <li><a href="/tasks">Найти задания</a></li>
                        <li><a href="/register">Стать исполнителем</a></li>
                    </ul>
                </div>
                <div class="col-md-3 mb-4">
                    <h5>Контакты</h5>
                    <ul class="list-unstyled">
                        <li><a href="mailto:support@vse-reshu.com">support@vse-reshu.com</a></li>
                        <li>
                            <div class="mt-3">
                                <a href="#" class="me-2"><i class="bi bi-facebook" style="font-size: 1.5rem;"></i></a>
                                <a href="#" class="me-2"><i class="bi bi-instagram" style="font-size: 1.5rem;"></i></a>
                                <a href="#" class="me-2"><i class="bi bi-telegram" style="font-size: 1.5rem;"></i></a>
                            </div>
                        </li>
                    </ul>
                </div>
            </div>
            <div class="row pt-3 border-top">
                <div class="col-md-6">
                    <p>&copy; 2023 Всё реши. Все права защищены.</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <a href="#" class="me-3">Политика конфиденциальности</a>
                    <a href="#">Пользовательское соглашение</a>
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>